# Option A (recommended for local dev / Codespaces): SQLite (creates ./linksnap.db)
DATABASE_URL=sqlite:///./linksnap.db
REDIS_HOST=localhost
REDIS_PORT=6379
BASE_URL=http://localhost:8000

# Option B (Docker / Postgres):
# DATABASE_URL=postgresql://postgres:password@db:5432/linksnap
# REDIS_HOST=redis
